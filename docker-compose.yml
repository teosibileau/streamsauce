---
services:
  postgres:
    image: postgres:15
    container_name: dbos_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-dbos}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-dbos}
      POSTGRES_DB: ${POSTGRES_DB:-dbos}
    ports:
      - "5432:5432"
    volumes:
      - dbos_pg_data:/var/lib/postgresql/data

volumes:
  dbos_pg_data:
